[package]
name = "rlox-burn"
version = "0.2.0"
edition = "2021"
license.workspace = true
description = "Burn backend for rlox neural network training"
repository.workspace = true
keywords.workspace = true
categories.workspace = true

[dependencies]
rlox-nn = { version = "0.2.0", path = "../rlox-nn" }
burn = { version = "0.16", features = ["autodiff", "ndarray", "wgpu"] }
rand = "0.9"
rand_chacha = "0.9"

[dev-dependencies]
